// This layout is superseded by app/layout.tsx — kept for reference only
import type { ReactNode } from 'react'

export default function StoreLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
